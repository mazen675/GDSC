import java.util.ArrayList;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        /*
        Cart myCart=new Cart();
        ArrayList<Product>books;
        ArrayList<Product>expBooks;
        ArrayList<Product>cheapBooks;
        ArrayList<Product>toys;

        myCart.currentProducts=(ArrayList<Product>) ListsGenerator.addDummyData();

        books=myCart.booksList();
        expBooks=myCart.expensiveBooksList();
        cheapBooks=myCart.cheapBooksList();
        toys=myCart.toysList();

        System.out.println(toys);

         */
    }
}